export const LOGIN = ({ commit }, value) => {
    commit('LOGIN', value)
}

export const DECREMENT = ({ commit }) => {
    commit('DECREMENT')
}

export const SAVEINFO = ({commit}) => {
    commit('SAVEINFO')
}